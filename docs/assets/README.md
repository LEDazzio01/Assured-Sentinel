# Dashboard Preview Placeholder

To generate a real screenshot:
1. Run `python calibration.py` to generate calibration data
2. Run `streamlit run dashboard.py`
3. Take a screenshot and save as `dashboard-preview.png`

Or use a screen recording tool to create a GIF.
